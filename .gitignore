# === Governance root ===
# Tracks portable canon (docs, AGENTS, .github). Nested project repos stay untracked.

# Independent project repos / local product (do not track)
/DnDApp/
/Phylactery-Bridge/
/gentle-ai/
/SkullRender/
/SkullRender-Agents/
/skullrender-mcp-skills/
/office-accelerator/
/teacher-daniela/
/phylactery/
/AgenteCrozz/
/solid-scrivener/
/Check-U/

# Local MCP smoke (may differ per machine). Ship: governance-portable/templates/mcp.json.example
.vscode/mcp.json

# --- Dependencies / build / caches ---
node_modules/
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
dist/
build/
*.log

# --- OS / editor noise ---
Thumbs.db
.DS_Store
*.swp

# --- AI tool caches ---
.aider*
.aider.tags.cache.v4/

# --- Secrets (never commit) ---
.env
*.env
.env.*
!*.env.example
